# dependency_proof_before_loadout

A skill that imports or references other components requires proof those dependencies exist in loadout BEFORE installation. The dependency_audit_lens discovered chain_verifier_recipe.md's missing Divergence/Convergence Lens after install — the revert still hit. Audit tools catch failures; they don't prevent them. Loadout admission requires dependency resolution, not post-hoc discovery.
