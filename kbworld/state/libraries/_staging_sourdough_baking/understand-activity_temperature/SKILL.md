# understand-activity_temperature

**CALL NUMBER:** `deep_fermentation.activity_temperature : sourdough_baking(1)`
**DEFINITION:** Temperature range that modulates fermentation vigor, with optimal range 24-28°C for balanced wild yeast and lactobacillus activity.

Invoke this skill to understand `activity_temperature` down to its primitives. The RELATIVE ROOT below is the least-fixed-point closure of everything it bundles from — the full import cone, grouped by the lib each prim comes from. Projected from a prover-typed KB (MAP/SWI-Prolog consistency gate): every reference below resolves.

## THE RELATIVE ROOT (the import cone, by lib)

### from `sourdough_baking`
- **fermentation_temperature** (d1): Environmental temperature during bulk and final proof that directly controls fermentation speed; warmer accelerates, cooler slows.

---
*Projected from the `sourdough baking` KB (298 concepts / 165 relations) — consistency-typed by MAP; the facet list after the colon IS the cross-lib dependency web.*